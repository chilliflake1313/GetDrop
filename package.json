{
  "name": "dropget",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "drop": "npm --prefix dropget/backend start",
    "start": "npm --prefix dropget/backend start",
    "dev": "npm --prefix dropget/backend run dev",
    "postinstall": "npm --prefix dropget/backend install"
  }
}
